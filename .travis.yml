language: go
go:
  - 1.10
  - 1.11
  - tip
before_install:
  - go get github.com/golang/dep/...
install:
  - $GOPATH/bin/dep ensure
script:
  - go test -race -coverprofile=coverage.txt -covermode=atomic github.com/ohtomi/gomoku
after_success:
  - bash <(curl -s https://codecov.io/bash)
